node_modules/
*.log

# Rendered output. The 4K master is ~280 MB and the intermediate frame dumps
# run to about a gigabyte; everything here comes back from `npm run flow`.
docs/.frames/
docs/.flow-frames/
docs/*.mp4
docs/flow.gif
docs/flow-preview.png

# Raw Natural Earth downloads, ~22 MB. `npm run geo` fetches these and trims
# them into the europe-*.geojson files, which are committed.
data/ne_10m_*.geojson

# Forecast data, regenerated by `npm run fetch`.
data/spread*.json
